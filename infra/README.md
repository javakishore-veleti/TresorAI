# infra

| Subdirectory | Purpose |
|---|---|
| [`local/`](./local) | Local-dev docker-compose stacks (postgres, redis, kafka). One compose file per tool. |
| [`cloudrun/`](./cloudrun) | Per-module Cloud Run deploy manifests (M2 scope). |

See `local/README.md` for day-to-day developer setup.
