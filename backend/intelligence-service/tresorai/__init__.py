"""TrésorAI intelligence-service package.

Layout:
  tresorai.data       — synthesis, downloaders, manifest, paths (ADR-0014)
  tresorai.storage    — cloud-agnostic adapters: GCS / S3 / Azure (ADR-0015)
  tresorai.models     — Classical ML + Deep Learning training/inference
  tresorai.agents     — Gemini agent loop, tool catalog, prompt registry
"""
