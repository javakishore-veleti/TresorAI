{
  "name": "tresorai",
  "version": "0.0.1",
  "private": true,
  "description": "TrésorAI monorepo — agentic SMB treasury & payment-fraud copilot. This package.json is the unified task runner; per-module commands live in their own package.json / pom.xml / pyproject.toml.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/javakishore-veleti/TresorAI"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup:initial:all": "npm run setup:conda:create && npm run setup:python:deps && npm run infra:up && npm run generate:favicons && npm run setup:initial:downloads",

    "setup:conda:create": "bash scripts/conda-create.sh",
    "setup:conda:remove": "bash scripts/conda-remove.sh",
    "env:activate": "echo 'Run: source ./scripts/conda-activate.sh'",
    "env:deactivate": "echo 'Run: source ./scripts/conda-deactivate.sh'",

    "setup:python:deps": "bash scripts/python-deps.sh",

    "infra:up": "bash infra/local/docker-all-up.sh",
    "infra:down": "bash infra/local/docker-all-down.sh",
    "infra:down:hard": "bash infra/local/docker-all-down-hard.sh",
    "infra:status": "bash infra/local/docker-all-status.sh",

    "dev:up": "bash scripts/dev-up.sh",
    "dev:down": "bash scripts/dev-down.sh",

    "generate:favicons": "node scripts/generate-favicons.js",

    "setup:initial:downloads": "echo 'Reference-data seeding runs from the admin portal: Administration -> Data Management -> Initial Downloads. CLI counterpart not implemented yet.'"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.33.5"
  }
}
